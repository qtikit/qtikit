{
  "name": "@qtikit/model",
  "packageManager": "yarn@3.1.0",
  "version": "0.0.4",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "tsc",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "devDependencies": {
    "typescript": "^4.4.4"
  }
}
