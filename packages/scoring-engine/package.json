{
  "name": "@qtikit/scoring-engine",
  "packageManager": "yarn@3.1.0",
  "version": "0.0.6",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "tsc",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "dependencies": {
    "@qtikit/model": "workspace:^"
  },
  "devDependencies": {
    "typescript": "^4.4.4"
  }
}
